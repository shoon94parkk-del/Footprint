async function get(url,timeout=15000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{signal:c.signal,redirect:'follow',headers:{'user-agent':'Mozilla/5.0','accept-language':'en-US,en;q=0.9'}});return{status:r.status,text:await r.text()}}finally{clearTimeout(t)}}
function around(text,needle,span=1100){const i=text.indexOf(needle);return i<0?'':text.slice(Math.max(0,i-span),Math.min(text.length,i+needle.length+span))}
(async()=>{try{const r=await get('https://search.jina.ai/app.js'),keys=['BASE_ORIGIN','Authorization','apiKey','API_KEY','chat/completions','localStorage.getItem','rateLimit','headers =','const headers','payload ='];const chunks={};for(const k of keys){const v=around(r.text,k);if(v)chunks[k]=v}console.log('[JINALOGIC]',JSON.stringify({status:r.status,bytes:r.text.length,chunks}))}catch(e){console.log('[JINALOGIC]',JSON.stringify({error:e.name+': '+e.message}))}})();
